# VITE_API_BASE_URL is no longer needed. The frontend calls a relative
# "/api" path (see src/services/api.js), which Vite's dev server proxies
# to the backend (see vite.config.js) - this works on any network
# (localhost, home Wi-Fi, a phone hotspot) with nothing to edit here.
